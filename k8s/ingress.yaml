apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: diabetes-mlservice
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  ingressClassName: nginx
  rules:
    - host: http://158.160.184.127:8110
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: diabetes-mlservice
                port:
                  number: 80
