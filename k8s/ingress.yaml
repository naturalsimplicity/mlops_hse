apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: diabetes-ml-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: diabetes.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: diabetes-ml-service
                port:
                  number: 8000
