apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diabetes-ml-chart.fullname" . }}-test-connection"
  labels:
    {{- include "diabetes-ml-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diabetes-ml-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
