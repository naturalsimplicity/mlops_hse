apiVersion: v1
kind: Service
metadata:
  name: {{ include "diabetes-ml-chart.fullname" . }}
  labels:
    {{- include "diabetes-ml-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "diabetes-ml-chart.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
